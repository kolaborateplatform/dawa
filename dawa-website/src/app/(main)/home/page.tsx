import React from 'react';
import HomePage from './HomePage';
import Layout from '@/components/layout';

const page = () => {
  return (
    <Layout>
      <HomePage />
    </Layout>
  );
};

export default page;
